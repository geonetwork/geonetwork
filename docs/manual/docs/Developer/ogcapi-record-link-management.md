# OGCAPI-Records Link Management


This is placeholder to be filled in later.  

Describe how links are generated (and annotated) in GN5.

This interacts with the Content Negotiation System.  One of the "hard" things is many return objects have sub-objects (i.e. `/items` and `/collections` endpoints).  These need to be annotated with "`alternative`" and "`self`" correctly.