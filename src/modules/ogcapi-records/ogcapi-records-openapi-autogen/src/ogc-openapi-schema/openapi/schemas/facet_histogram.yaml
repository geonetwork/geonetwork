allOf:
  - $ref: 'facet_common.yaml'
  - type: object
    properties:
      bucketType:
        type: string
        enum:
          - fixedInterval
          - fixedBucketCount
      "x-elastic-datatype":
        type: string
        enum:
          - number
          - date
      "x-interval-number":
        type: number
      "x-interval-calendar-interval":
        type: string
      "x-minimum-doc-count":
        type: integer

