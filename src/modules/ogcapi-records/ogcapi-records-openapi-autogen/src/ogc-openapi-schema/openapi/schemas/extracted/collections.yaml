#https://schemas.opengis.net/ogcapi/features/part1/1.0/openapi/schemas/collections.yaml
---
type: object
required:
  - links
  - collections
properties:
  links:
    type: array
    xml:
      wrapped: true
      name: links
    items:
      $ref: ../link.yaml
  collections:
    type: array
    items:
      $ref: collection.yaml
