#
# This is not actually used - it will be replaced by GN5's index record
#
# See the codegen config in the pom.xml
#
# See org.geonetwork.index.model.record.IndexRecord.java
---
type: object
description:
  Placeholder - will actually use GN5's IndexRecord
required:
  - code
properties:
  code:
    type: string
