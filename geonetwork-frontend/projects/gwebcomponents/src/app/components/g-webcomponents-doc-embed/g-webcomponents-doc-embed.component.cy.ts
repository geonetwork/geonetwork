import { GWebcomponentsDocEmbedComponent } from './g-webcomponents-doc-embed.component';

describe('GWebcomponentsDocEmbedComponent', () => {
  it('should mount', () => {
    cy.mount(GWebcomponentsDocEmbedComponent);
  });
});
