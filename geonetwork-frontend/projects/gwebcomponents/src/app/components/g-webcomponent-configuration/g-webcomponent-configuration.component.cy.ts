import { GWebcomponentConfigurationComponent } from './g-webcomponent-configuration.component';

describe('GWebcomponentConfigurationComponent', () => {
  it('should mount', () => {
    cy.mount(GWebcomponentConfigurationComponent);
  });
});
