import { GcDataResultsTableComponent } from './gc-data-results-table.component';

describe('GDataResultsTableComponent', () => {
  it('should mount', () => {
    cy.mount(GcDataResultsTableComponent);
  });
});
