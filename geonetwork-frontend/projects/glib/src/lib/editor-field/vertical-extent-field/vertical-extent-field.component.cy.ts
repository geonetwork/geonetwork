import { VerticalExtentFieldComponent } from './vertical-extent-field.component';

describe('VerticalExtentFieldComponent', () => {
  it('should mount', () => {
    cy.mount(VerticalExtentFieldComponent);
  });
});
