import { TemporalExtentFieldComponent } from './temporal-extent-field.component';

describe('TemporalExtentFieldComponent', () => {
  it('should mount', () => {
    cy.mount(TemporalExtentFieldComponent);
  });
});
