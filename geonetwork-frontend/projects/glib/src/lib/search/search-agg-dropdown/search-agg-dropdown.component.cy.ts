import { SearchAggDropdownComponent } from './search-agg-dropdown.component'

describe('SearchAggDropdownComponent', () => {
  it('should mount', () => {
    cy.mount(SearchAggDropdownComponent)
  })
})