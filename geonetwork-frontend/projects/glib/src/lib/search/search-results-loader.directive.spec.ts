import { SearchResultsLoaderDirective } from './search-results-loader.directive';

describe('SearchResultsLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchResultsLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
