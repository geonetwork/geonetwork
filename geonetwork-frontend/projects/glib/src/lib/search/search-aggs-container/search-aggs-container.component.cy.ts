import { SearchAggsContainerComponent } from './search-aggs-container.component';

describe('SearchAggsContainerComponent', () => {
  it('should mount', () => {
    cy.mount(SearchAggsContainerComponent);
  });
});
