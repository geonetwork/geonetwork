import { DataAnalysisPanelComponent } from './data-analysis-panel.component';

describe('DataAnalysisPanelComponent', () => {
  it('should mount', () => {
    cy.mount(DataAnalysisPanelComponent);
  });
});
