import { TemplatesSelectorComponent } from './templates-selector.component';

describe('TemplatesSelectorComponent', () => {
  it('should mount', () => {
    cy.mount(TemplatesSelectorComponent);
  });
});
