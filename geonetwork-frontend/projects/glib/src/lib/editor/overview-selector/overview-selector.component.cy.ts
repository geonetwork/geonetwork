import { OverviewSelectorComponent } from './overview-selector.component';

describe('OverviewSelectorComponent', () => {
  it('should mount', () => {
    cy.mount(OverviewSelectorComponent);
  });
});
