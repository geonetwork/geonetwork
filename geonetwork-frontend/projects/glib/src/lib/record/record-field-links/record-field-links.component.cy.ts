import { RecordFieldLinksComponent } from './record-field-links.component';

describe('RecordFieldLinksComponent', () => {
  it('should mount', () => {
    cy.mount(RecordFieldLinksComponent);
  });
});
