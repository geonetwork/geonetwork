import { GJsonpathPipe } from './g-jsonpath.pipe';

describe('GJsonpathPipe', () => {
  it('create an instance', () => {
    const pipe = new GJsonpathPipe();
    expect(pipe).toBeTruthy();
  });
});
