import { UrlPlaceholderPipe } from './url-placeholder.pipe';

describe('UrlPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
